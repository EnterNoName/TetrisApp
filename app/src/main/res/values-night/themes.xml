<resources>
    <!-- Base application theme. -->
    <style name="Theme.Tetris" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/black</item>
        <item name="colorPrimaryVariant">@color/dark_grey</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/white_transparent</item>
        <item name="colorSecondaryVariant">@color/grey_transparent</item>
        <item name="colorOnSecondary">@color/black_transparent</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/transparent</item>
        <!-- Customize your theme here. -->
        <item name="android:alertDialogTheme">@style/LightDialogTheme</item>
        <item name="alertDialogTheme">@style/LightDialogTheme</item>
    </style>

    <style name="Theme.Tetris.Fullscreen" parent="Theme.Tetris">
        <item name="android:actionBarStyle">@style/Widget.Theme.Tetris.ActionBar.Fullscreen</item>
        <item name="android:windowActionBarOverlay">true</item>
        <item name="android:windowBackground">@null</item>
    </style>

    <style name="LightDialogTheme" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="colorPrimary">@color/black</item>
    </style>
</resources>