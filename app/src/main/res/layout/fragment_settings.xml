<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:gravity="center_vertical"
    android:orientation="horizontal">
    <ImageView
        android:id="@+id/ivSettingIcon"
        android:layout_width="32dp"
        android:layout_height="32dp"
        android:layout_marginEnd="16dp"
        app:tint="?colorOnPrimary"
        tools:src="@drawable/ic_round_videogame_controller_24" />

    <TextView
        android:id="@+id/tvSettingName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="?colorOnPrimary"
        android:fontFamily="@font/fira_sans_medium"
        android:textSize="12sp"
        tools:text="Control Scheme" />

    <View
        android:layout_width="4dp"
        android:layout_height="4dp"
        android:layout_marginHorizontal="8dp"
        android:background="@drawable/divider_bullet"
        app:tint="?colorOnPrimary" />

    <TextView
        android:id="@+id/tvSettingValue"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="?colorOnPrimary"
        android:fontFamily="@font/fira_sans_medium"
        android:textSize="12sp"
        tools:text="Touch" />
</LinearLayout>